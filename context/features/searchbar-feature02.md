# Searchbar Feature 02

## Overview

Add switch toggle to searchbar. It can stwitch search mode from normal searching (title, contents, collection, tags) to semantic searching.

## Requirements

- Toggle is located at left of searchbar
- Make result blocks' style like now it looks like
- Normal mode shows matching notes by title, notes by content, collections, tags
- Semantic mode shows semantic matching notes
- Use Shadcn/UI
- Add apply search button to searchbar, no auto search after typing
