# Searchbar Feature 01

## Overview

Add collections and tags title matching search in searchbar.

## Requirements

- Results locate below the semantic search
- Only title matching search, no semantic for these 2
- Click result to corresponding page
- Show title, notes counts instead of updated time
- Create new component if need
