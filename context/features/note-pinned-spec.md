# Pinned Note Spec

## Overview

Build pinned note page that user can access thru quick access block of sidebar. This page shows all pinned note. The note card conponent is @/src/components/dashboard/AppNoteCard.tsx, and it might need to build or modify API in @/src/lib/db/notes.ts.

## Requirements

- Check if it need to build a new API or modify the one we have in @/src/lib/db/notes.ts.
- If We just do modification, check @/src/conponents/dashboard/AppPinnedNotes.tsx that it just show 2 note cards.
- The layout would look like @/src/app/dashboard/allnotes/pages.tsx.

## References
